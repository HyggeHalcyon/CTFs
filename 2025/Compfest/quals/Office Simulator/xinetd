service challenge
{
    disable     = no
    type        = UNLISTED
    socket_type = stream
    protocol    = tcp
    port        = 1338
    wait        = no
    user        = user
    server      = /run.sh
}